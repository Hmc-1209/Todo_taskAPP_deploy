pipeline {
    agent any

    stages {
        stage('Build') {
            // Put docker stop, rm, build and run here
            steps {
                echo 'Building...'
            }
        }

        stage('Test') {
            // Put test files here
            steps {
                echo 'Running tests...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying...'
            }
        }
    }
}
